import React, { Component, Fragment } from 'react';
import styled from 'styled-components';

export default class SpiralGame extends Component{
    constructor(props){
        this.state = {

        };
    }

    render(){
        return(
            
        );
    }
}